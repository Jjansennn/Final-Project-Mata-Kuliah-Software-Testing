[pytest]
testpaths = tests/backend
pythonpath = src/backend tests/backend
addopts = --cov=app --cov-report=term-missing --cov-fail-under=100
filterwarnings =
    ignore::coverage.exceptions.CoverageWarning

[coverage:run]
source = app
